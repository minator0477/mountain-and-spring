{
  "name": "next-visit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "uv run uvicorn src.server:app --reload --host 0.0.0.0 --port 8000",
    "dev:spring": "uv run uvicorn src.spring_server:app --reload --host 0.0.0.0 --port 8001",
    "build": "vite build",
    "start": "APP_ENV=production uv run python src/server.py"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
